## 1. OpenSpec 与测试准备

- [x] 1.1 运行 `openspec validate add-minesweeper-game --no-color`，确认提案、设计和规格有效。
- [x] 1.2 确认前端没有现成单元测试框架时，采用 Node 运行纯逻辑测试，不新增测试框架依赖。

## 2. 扫雷核心逻辑

- [x] 2.1 新增扫雷核心测试，覆盖新局尺寸、默认状态和难度配置。
- [x] 2.2 实现 `createGame`，支持初级、中级、高级和自定义参数限制。
- [x] 2.3 新增首次点击安全测试，覆盖首次点击格安全、空间足够时九宫格安全、空间不足时至少点击格安全。
- [x] 2.4 实现延迟布雷、相邻格计算和数字计算。
- [x] 2.5 新增翻开测试，覆盖数字格显示、空白格扩散和已标旗格不可翻开。
- [x] 2.6 实现 `revealCell` 的翻开、扩散和已标旗保护逻辑。
- [x] 2.7 新增标旗测试，覆盖标旗、取消旗标、已翻开格不可标旗和剩余雷数。
- [x] 2.8 实现 `toggleFlag` 和剩余雷数统计。
- [x] 2.9 新增胜负测试，覆盖踩雷失败、全部安全格翻开胜利、插旗数量不直接胜利、结束后不可继续操作。
- [x] 2.10 实现胜利、失败、爆炸格、错误旗标和胜利自动标旗逻辑。
- [x] 2.11 运行扫雷核心逻辑测试，确认全部通过。

## 3. 前端页面

- [x] 3.1 新增 `ruoyi-ui/src/views/game/minesweeper/index.vue` 页面骨架，包含工具栏、状态区和棋盘区。
- [x] 3.2 接入难度选择、自定义行列雷数输入和重新开始操作。
- [x] 3.3 接入左键翻开、右键标旗和长按标旗交互。
- [x] 3.4 实现计时、剩余雷数、已翻开数量和局状态展示。
- [x] 3.5 完成棋盘格样式，区分未翻开、已翻开、数字、旗标、地雷、爆炸格和错误旗标。
- [x] 3.6 检查桌面端和窄屏布局，确保大棋盘可横向滚动且格子尺寸稳定。

## 4. 菜单接入

- [x] 4.1 检查 `sql/ry_20250522.sql` 现有 `sys_menu` ID，选择未占用 ID。
- [x] 4.2 在 SQL 初始化脚本中新增“休闲游戏”目录和“扫雷”菜单。
- [x] 4.3 确认菜单组件路径为 `game/minesweeper/index`，权限标识为 `game:minesweeper:view`。

## 5. 验证与归档

- [x] 5.1 运行扫雷核心逻辑测试并记录结果。
- [x] 5.2 运行 `npm run build:prod`，确认前端构建通过。
- [x] 5.3 运行 `openspec validate add-minesweeper-game --no-color`，确认变更有效。
- [x] 5.4 运行 `openspec status --change add-minesweeper-game --json`，确认工件完整。
- [x] 5.5 代码、测试和规格对齐后，归档 `add-minesweeper-game`。
- [x] 5.6 更新 `.superpowers-memory/CURRENT_STATE.md` 并新增一条会话记录。
