## 1. OpenSpec 与测试准备

- [x] 1.1 运行 `openspec validate add-chinese-checkers-game --no-color`，确认提案、设计和规格有效。
- [x] 1.2 确认前端测试策略，沿用 Node 原生断言测试纯 JavaScript 游戏核心，不新增测试框架依赖。

## 2. 中国跳棋核心逻辑

- [x] 2.1 新增中国跳棋核心测试，覆盖棋盘落点数量、双方初始棋子数量和初始营地。
- [x] 2.2 实现 `createBoard` 和 `createGame`，生成 121 个有效落点、双方营地和初始棋子。
- [x] 2.3 新增棋子选择测试，覆盖只能选择当前玩家棋子、不能选择对方棋子和空落点。
- [x] 2.4 实现 `selectPiece`，返回选中棋子和合法落点。
- [x] 2.5 新增一步移动测试，覆盖相邻空位移动、被占用落点和棋盘外无效目标。
- [x] 2.6 实现一步移动、回合切换和步数更新。
- [x] 2.7 新增跳跃移动测试，覆盖有中间棋子可跳跃、无中间棋子不可跳跃、跳跃不移除棋子。
- [x] 2.8 实现跳跃移动和合法跳跃计算。
- [x] 2.9 新增连续跳跃测试，覆盖保持同一棋子、禁止一步移动、禁止改选其他棋子、主动结束回合和无后续跳跃自动结束。
- [x] 2.10 实现连续跳跃状态、`endTurn` 和跳跃链限制。
- [x] 2.11 新增胜负测试，覆盖玩家一胜利、玩家二胜利和结束后不可继续移动。
- [x] 2.12 实现胜负判断和结束后操作保护。
- [x] 2.13 运行中国跳棋核心逻辑测试，确认全部通过。

## 3. 前端页面

- [x] 3.1 新增 `ruoyi-ui/src/views/game/chinese-checkers/index.vue` 页面骨架，包含顶部状态区、棋盘区和双方状态区。
- [x] 3.2 接入棋子选择、合法落点高亮、一步移动、跳跃移动、连续跳跃提示和结束回合按钮。
- [x] 3.3 接入重新开始、当前回合、步数、胜利提示和双方目标完成情况展示。
- [x] 3.4 完成棋盘和棋子样式，确保星形布局稳定、落点尺寸稳定、焦点状态清晰、窄屏可用。

## 4. 菜单接入

- [x] 4.1 检查现有扫雷菜单 ID 和初始化 SQL，选择跳棋未占用菜单 ID。
- [x] 4.2 新增 `sql/chinese_checkers_menu.sql`，复用或补充“休闲游戏”目录并新增“跳棋”菜单。
- [x] 4.3 确认菜单组件路径为 `game/chinese-checkers/index`，权限标识为 `game:chinese-checkers:view`，并为 `common` 角色补充授权。

## 5. 验证与收尾

- [x] 5.1 运行中国跳棋核心逻辑测试并记录结果。
- [x] 5.2 运行 `npm run build:prod`，确认前端构建通过。
- [x] 5.3 运行 `openspec validate add-chinese-checkers-game --no-color`，确认变更有效。
- [x] 5.4 运行 `openspec status --change add-chinese-checkers-game --json`，确认工件完整。
- [x] 5.5 更新 `.superpowers-memory/CURRENT_STATE.md` 并新增一条会话记录。
