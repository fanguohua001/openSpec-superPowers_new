## 1. 数据库与初始化数据

- [x] 1.1 在 SQL 脚本中新增 `approval_process` 流程定义表，包含业务类型、流程名称、状态和审计字段。
- [x] 1.2 在 SQL 脚本中新增 `approval_node` 节点定义表，包含流程 ID、节点名称、节点顺序和状态字段。
- [x] 1.3 在 SQL 脚本中新增 `approval_node_approver` 节点审批人表，支持 `USER` 和 `ROLE` 两类审批人规则。
- [x] 1.4 在 SQL 脚本中新增 `approval_instance` 审批实例表，保存业务类型、业务主键、业务标题、发起人、当前节点和实例状态。
- [x] 1.5 在 SQL 脚本中新增 `approval_task` 审批任务表，保存实例、节点、审批人、任务状态和处理时间。
- [x] 1.6 在 SQL 脚本中新增 `approval_record` 审批记录表，保存动作、意见、操作人、原节点、目标节点和操作时间。
- [x] 1.7 为审批相关表补充按业务引用、发起人、审批人、实例状态、任务状态查询所需索引。
- [x] 1.8 新增审批管理菜单和按钮权限初始化数据，包含流程定义、我的申请、待我审批、查询、添加、修改、删除、启用、审批处理权限。

## 2. 后端测试

- [x] 2.1 新增流程定义启用校验测试，覆盖无节点、节点无审批人时禁止启用。
- [x] 2.2 新增发起审批测试，覆盖按业务类型匹配启用流程并创建首节点待办。
- [x] 2.3 新增指定用户审批人测试，覆盖只给指定用户生成待办。
- [x] 2.4 新增指定角色审批人测试，覆盖按发起时角色成员展开待办。
- [x] 2.5 新增或签通过测试，覆盖任一候选人同意后进入下一节点并关闭同节点其他待办。
- [x] 2.6 新增最后节点通过测试，覆盖审批实例状态变为已通过。
- [x] 2.7 新增非首节点驳回测试，覆盖实例退回上一节点并重新生成上一节点待办。
- [x] 2.8 新增首节点驳回测试，覆盖实例状态变为已驳回。
- [x] 2.9 新增待办归属校验测试，覆盖用户不能处理不属于自己的任务。
- [x] 2.10 新增我的申请隔离测试，覆盖普通用户不能查看他人发起的审批详情。
- [x] 2.11 新增审批记录测试，覆盖同意、驳回均写入记录并保留节点变化。

## 3. 后端实现

- [x] 3.1 在 `ruoyi-system` 新增审批流程、节点、节点审批人、实例、任务、记录 domain 对象。
- [x] 3.2 在 `ruoyi-system` 新增审批 mapper 接口和 MyBatis XML。
- [x] 3.3 在 `ruoyi-system` 新增流程定义 service，支持列表、详情、新增、修改、删除、启用、停用和完整性校验。
- [x] 3.4 在 `ruoyi-system` 新增节点配置 service，支持维护线性节点和指定用户/角色审批人。
- [x] 3.5 在 `ruoyi-system` 新增审批发起 service，支持通过业务类型、业务主键、业务标题创建审批实例。
- [x] 3.6 在 `ruoyi-system` 实现审批任务生成逻辑，指定用户直接生成任务，指定角色按当前角色成员展开为用户任务。
- [x] 3.7 在 `ruoyi-system` 实现审批同意逻辑，包含或签通过、关闭同节点其他任务、进入下一节点或完成实例。
- [x] 3.8 在 `ruoyi-system` 实现审批驳回逻辑，包含非首节点退回上一节点、首节点转为已驳回。
- [x] 3.9 在 `ruoyi-system` 实现我的申请、待我审批、审批详情和审批记录查询。
- [x] 3.10 在 `ruoyi-system` 实现审批动作资格校验，确保只有待办归属用户可以处理。
- [x] 3.11 在 `ruoyi-admin` 新增审批流程定义 Controller。
- [x] 3.12 在 `ruoyi-admin` 新增审批实例和审批任务 Controller。
- [x] 3.13 为审批接口添加 `@PreAuthorize` 权限注解和必要的 `@Log` 操作日志。

## 4. 前端实现

- [x] 4.1 新增 `ruoyi-ui/src/api/approval/process.js`，封装流程定义和节点配置接口。
- [x] 4.2 新增 `ruoyi-ui/src/api/approval/instance.js`，封装发起审批、我的申请、审批详情接口。
- [x] 4.3 新增 `ruoyi-ui/src/api/approval/task.js`，封装待我审批、同意、驳回接口。
- [x] 4.4 新增流程定义管理页面，支持查询、新增、修改、删除、启用、停用和节点审批人配置。
- [x] 4.5 新增我的申请页面，支持查询本人发起的审批实例并打开详情。
- [x] 4.6 新增待我审批页面，支持查询当前用户待办并执行同意或驳回。
- [x] 4.7 新增审批详情视图，展示业务标题、业务类型、当前状态、当前节点、任务信息和审批记录。
- [x] 4.8 确认审批相关页面按钮使用 `v-hasPermi` 控制，与后端权限标识一致。

## 5. 集成边界

- [x] 5.1 提供业务模块发起审批的 service 方法或 REST 接口，入参包含 `businessType`、`businessId`、`businessTitle`。
- [x] 5.2 提供按业务类型和业务主键查询审批实例状态的方法，便于后续业务模块接入。
- [x] 5.3 编写首个示例或说明，标明业务模块如何发起审批以及如何读取审批状态。

## 6. 验证

- [x] 6.1 运行后端审批相关单元测试，确认新增测试通过。
- [x] 6.2 运行 Maven 编译或测试命令，确认后端构建通过。
- [x] 6.3 运行前端构建命令，确认前端构建通过。
- [x] 6.4 手工验证管理员可以创建流程、配置节点、配置指定用户和指定角色审批人。
- [x] 6.5 手工验证普通用户发起审批后，首节点候选审批人能在待我审批看到任务。
- [x] 6.6 手工验证或签节点一人同意后，同节点其他待办不可继续处理。
- [x] 6.7 手工验证非首节点驳回后，审批实例退回上一节点继续流转。
- [x] 6.8 手工验证首节点驳回后，审批实例变为已驳回。
- [x] 6.9 运行 `openspec validate add-custom-approval-flow --no-color`，确认 OpenSpec 变更有效。
