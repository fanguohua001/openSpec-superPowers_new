<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.AiChatSessionMapper">

    <resultMap type="AiChatSession" id="AiChatSessionResult">
        <id     property="sessionId"  column="session_id"  />
        <result property="userId"     column="user_id"     />
        <result property="title"      column="title"       />
        <result property="model"      column="model"       />
        <result property="status"     column="status"      />
        <result property="createBy"   column="create_by"   />
        <result property="createTime" column="create_time" />
        <result property="updateBy"   column="update_by"   />
        <result property="updateTime" column="update_time" />
        <result property="remark"     column="remark"      />
    </resultMap>

    <sql id="selectAiChatSessionVo">
        select session_id, user_id, title, model, status, create_by, create_time, update_by, update_time, remark
        from ai_chat_session
    </sql>

    <select id="selectAiChatSessionById" parameterType="Long" resultMap="AiChatSessionResult">
        <include refid="selectAiChatSessionVo"/>
        where session_id = #{sessionId}
    </select>

    <select id="selectAiChatSessionList" parameterType="AiChatSession" resultMap="AiChatSessionResult">
        <include refid="selectAiChatSessionVo"/>
        <where>
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="title != null and title != ''">
                AND title like concat('%', #{title}, '%')
            </if>
        </where>
        order by update_time desc, create_time desc, session_id desc
    </select>

    <insert id="insertAiChatSession" parameterType="AiChatSession" useGeneratedKeys="true" keyProperty="sessionId">
        insert into ai_chat_session(user_id, title, model, status, create_by, create_time, remark)
        values(#{userId}, #{title}, #{model}, #{status}, #{createBy}, sysdate(), #{remark})
    </insert>

    <update id="updateAiChatSession" parameterType="AiChatSession">
        update ai_chat_session
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="model != null">model = #{model},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_time = sysdate()
        </set>
        where session_id = #{sessionId}
    </update>

    <update id="deleteAiChatSessionByIds">
        update ai_chat_session
        set status = '1', update_time = sysdate()
        where user_id = #{userId}
        and session_id in
        <foreach collection="sessionIds" item="sessionId" open="(" separator="," close=")">
            #{sessionId}
        </foreach>
    </update>

</mapper>
