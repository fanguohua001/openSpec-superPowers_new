<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.AiChatMessageMapper">

    <resultMap type="AiChatMessage" id="AiChatMessageResult">
        <id     property="messageId"    column="message_id"    />
        <result property="sessionId"    column="session_id"    />
        <result property="userId"       column="user_id"       />
        <result property="role"         column="role"          />
        <result property="content"      column="content"       />
        <result property="status"       column="status"        />
        <result property="errorMessage" column="error_message" />
        <result property="createTime"   column="create_time"   />
    </resultMap>

    <sql id="selectAiChatMessageVo">
        select message_id, session_id, user_id, role, cast(content as char) as content, status, error_message, create_time
        from ai_chat_message
    </sql>

    <select id="selectAiChatMessageList" parameterType="AiChatMessage" resultMap="AiChatMessageResult">
        <include refid="selectAiChatMessageVo"/>
        <where>
            <if test="sessionId != null">
                AND session_id = #{sessionId}
            </if>
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        order by create_time asc, message_id asc
    </select>

    <select id="selectRecentMessages" resultMap="AiChatMessageResult">
        <include refid="selectAiChatMessageVo"/>
        where session_id = #{sessionId}
        and status = '0'
        order by create_time desc, message_id desc
        limit #{limit}
    </select>

    <insert id="insertAiChatMessage" parameterType="AiChatMessage" useGeneratedKeys="true" keyProperty="messageId">
        insert into ai_chat_message(session_id, user_id, role, content, status, error_message, create_time)
        values(#{sessionId}, #{userId}, #{role}, #{content}, #{status}, #{errorMessage}, sysdate())
    </insert>

    <update id="updateAiChatMessage" parameterType="AiChatMessage">
        update ai_chat_message
        <set>
            <if test="content != null">content = #{content},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="errorMessage != null">error_message = #{errorMessage},</if>
        </set>
        where message_id = #{messageId}
    </update>

</mapper>
